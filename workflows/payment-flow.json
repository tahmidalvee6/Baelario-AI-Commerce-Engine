{
  "nodes": [
    {
      "name": "Payment Detect",
      "type": "n8n-nodes-base.function",
      "parameters": {
        "functionCode": "const msg = $json.message;\nconst trx = msg.match(/\\b\\w{8,15}\\b/);\nreturn [{ json: { trx_id: trx ? trx[0] : null } }];"
      },
      "position": [400, 300]
    },
    {
      "name": "Update Sheet",
      "type": "n8n-nodes-base.googleSheets",
      "parameters": {
        "operation": "update"
      },
      "position": [600, 300]
    }
  ]
}
